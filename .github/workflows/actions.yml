name: Private Action
on: push
jobs:
  testing-action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ls -la
      - uses: ./.github/actions/hello
        id: hello
        with:
          who-to-greet: "Peter Yefi"
      - run: |
          echo "Time: ${{ steps.hello.outputs.time }}"
          echo $HELLO
